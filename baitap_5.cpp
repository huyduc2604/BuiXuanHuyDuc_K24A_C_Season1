#include <stdio.h>
int length = 9,
	width = 4,
	perimeter = length * 2 + width * 2,
	area = length * width;
int main() {
	printf ("Perimeter = %d\n", perimeter );
	printf ( "area= %d", area);
	return 0;
}
	
